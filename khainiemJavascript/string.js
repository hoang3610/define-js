/**
 * Chuỗi trong Javascript
 * 
 * 1. Tạo chuỗi
 *      - Các cách tạo chuỗi
 *      - Nên dùng cách nào? Lý do ?
 *      - Kiểm tra data type
 * 2. Một số case sử dụng backslash (\)
 * 3. Xem độ dài chuỗi
 * 4. Chú ý độ dài khi viết Code --> ko nên viết code quá 80 kí tự trên 1 dòng
 * 5. Template string ES6
 */

// Tạo chuỗi --> Có 2 cách
var fullName = "Van Hoang"; // thường sử dụng
var fullName1 = new String('Van Hoang') // tạo ra 1 đối tượng Object nên ít được sử dụng

// Xem độ dài chuỗi
console.log(fullName.length)
