/**
 * Return trong hàm - Javascript cơ bản
 * Nếu trong hàm mà không có return => thì javascript sẽ trả về undefind
 * hoặc trong hàm return về underfind
 * return có thể trả về bất cứ kiểu dữ gì luôn
 */

function cong(a, b) {
    return a + b;
}

var result = cong(2, 8);

console.log(result);